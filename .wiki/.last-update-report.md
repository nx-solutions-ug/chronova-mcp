# Wiki Updated

Run completed at 2026-07-20T17:52:53.060Z.

## Updated pages

- `.wiki/domain/errors.md`

  > I've completed my discovery. The key source change since the last wiki update is the extraction of duplicated tool error handling into a shared `formatToolError()` utility in `src/lib/errors.ts` (commit bb430c2). All four tool handlers now call `formatToolError(error)` instead of inlining the error-response construction. The `src/version.ts` extraction is already documented, and the `for...in` perf change is internal. The two pages directly affected are `domain/errors.md` (which describes the to…

- `.wiki/architecture/overview.md`

  > Now updating `architecture/overview.md` to reflect the shared `formatToolError` utility in the tool registration pattern section:

- `.wiki/architecture/overview.md`

  > Now let me update the source map table in `architecture/overview.md` to mention `src/version.ts` (extracted in commit d05faf7, already referenced in prose but not in the table) and add `formatToolError` to the errors.ts row:


## Summary

This update run edited 3 pages.
